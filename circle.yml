machine:
    services:
        - docker

dependencies:
    pre:
        - docker pull elasticsearch:5.0
    override:
        - composer global require "fxp/composer-asset-plugin:^1.1"
        - composer install --prefer-dist --no-interaction
        - docker run -p 9200:9200 -d elasticsearch:5.0 -Etransport.host=127.0.0.1
